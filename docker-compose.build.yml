services:
  grok2api:
    build:
      context: .
      dockerfile: Dockerfile
      # Uncomment to pre-install Playwright Chromium for auto-registration:
      # args:
      #   INSTALL_BROWSER: "true"
    image: grok2api:local
